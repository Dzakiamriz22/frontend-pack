---
description: Create or extend design system. Tokens, theme, component patterns, and shadcn/ui customization.
---

Load the design-system, color-system, typography, spacing, and tailwind skills. Ask the user if they're creating a new design system or extending an existing one. Generate: token definitions (colors, spacing, typography, radii, shadows), theme (light + dark), tailwind.config.ts with custom theme, globals.css with CSS variables, shadcn/ui customization. Include component variant patterns.
