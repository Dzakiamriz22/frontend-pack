---
description: Generate authentication flows. Login, register, forgot password, reset password, and OAuth pages.
---

Load the design, react, nextjs, shadcn, forms, and accessibility skills. Ask the user which auth page(s): login, register, forgot-password, reset-password, or all. Generate complete auth pages with: centered card layout (max-w-md), React Hook Form + Zod validation, error messages per field, submit button with loading state, links between auth pages (Login ↔ Register), OAuth buttons (GitHub, Google), dark mode. Include skeleton loader, error state for failed auth. Server action for form submission. Accessible: proper labels, error roles, focus management.
