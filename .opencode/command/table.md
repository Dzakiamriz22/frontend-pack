---
description: Generate data tables. CRUD tables, sortable columns, search, pagination, and responsive mobile card views.
---

Load the design, shadcn, tables, react, and performance skills. Ask the user for the data model/columns. Generate a full data table with: TanStack Table, search input, column sorting, pagination (client or server), row actions dropdown (Edit, Delete, View), batch selection if needed. Responsive: card layout on mobile. Include: loading skeleton rows, empty state, error state with retry. TanStack Query for data fetching. Dark mode. Optional: inline editing, row expansion.
